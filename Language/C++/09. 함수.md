```cpp
int Add (int a, int b) {
	return a + b; // 반환값
}

// 이름이 똑같은 함수를 만들 수 있고, 파라미터의 개수는 달라야 한다. (오버로딩)
void Add (int a, int b, int *c) {
	*c = a + b // 해당 주소에 결과를 직접 넣기 때문에 적용됨
	// a와 b는 복삿값, c는 주소기에 참조값
}

// 함수 하나는 기본적으로 return이 하나기 때문에, 함수 하나에서
// return을 여러개 받고 싶다면 포인터를 자주 사용한다.
```